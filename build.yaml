targets:
  $default:
    builders:
      drift_dev:
        options:
          sql:
            dialect: sqlite
          store_date_time_values_as_text: false
          generate_connect_constructor: false
